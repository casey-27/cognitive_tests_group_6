Note on file history:
As the first version of the spatial test got deleted due to having the same name as the version 1.2, files containing both version 1 and version 1.2 were added.
The files contain the same code as their respective versions but are renamed for the clarity of the version history.
